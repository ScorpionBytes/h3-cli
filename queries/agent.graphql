query q($name: String!) {
    agent(name:$name) {
        ...AgentScalarFragment
    }
}
  
fragment AgentScalarFragment on Agent {
    uuid
    name
    uname
    log_file
    last_heartbeat_at
    last_heartbeat_time_ago
    row_created_at
}
