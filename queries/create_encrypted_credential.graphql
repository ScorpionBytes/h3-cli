mutation m($input: InjectedCredentialInput!) {
    create_encrypted_credential(input:$input) {
        encrypted_credential {
            ...EncryptedCredentialFragment
        }
        encrypted_secret
    }
}

fragment EncryptedCredentialFragment on EncryptedCredential {
    uuid
    key_type
    username
    ip
    aws_access_key_id
}